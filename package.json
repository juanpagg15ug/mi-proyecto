{
  "scripts": {
    "validate": "node --check public/js/app.js && node --check public/js/components/header.js && node --check public/js/views/catalogovView.js && node --check public/js/views/caseDetailView.js && node --check public/js/views/eventView.js",
    "emulators": "npx firebase-tools@latest emulators:start --only hosting,firestore",
    "deploy:rules": "npx firebase-tools@latest use praxis-prio && npx firebase-tools@latest deploy --only firestore:rules",
    "deploy:hosting": "npx firebase-tools@latest use praxis-prio && npx firebase-tools@latest deploy --only hosting"
  },
  "dependencies": {
    "firebase": "^12.17.1"
  }
}
